// Point d'entrée du widget ICSMulti intégrable
// Stub — implémentation complète à venir dans la phase 6

export interface OptionsWidget {
  container: string;
  titre?: string;
  theme?: "light" | "dark" | "auto";
  lang?: string;
}

/**
 * Initialise le widget ICSMulti dans le conteneur spécifié.
 * Stub — implémentation complète à venir.
 */
export function init(_options: OptionsWidget): void {
  // à implémenter
}
